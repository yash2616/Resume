# ATS test resume

This is an isolated, single-column rendering of the root CV. It preserves the source content while avoiding CSS grids, two-column reading order, and visual-only separators.

The accompanying PDF is generated only after print rendering and text-extraction checks.
